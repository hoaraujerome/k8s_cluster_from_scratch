kind: KubeProxyConfiguration
apiVersion: kubeproxy.config.k8s.io/v1alpha1
clientConnection:
  kubeconfig: "/root/{{ kube_proxy_name }}.kubeconfig"
mode: "iptables"
clusterCIDR: "{{ pods_cidr_range }}"
